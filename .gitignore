# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright's own generated run output (traces, last-run state)
test-results
playwright-report
blob-report
playwright/.cache

# Secrets. Nothing currently reads one (confirmed: no import.meta.env/
# process.env usage anywhere in src/ or scripts/), but ignored
# defensively so an accidental one never gets committed.
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
